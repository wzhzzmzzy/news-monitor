# 复制到 config/local/llm.yaml，并在主配置 includes 中引用。密钥只从环境变量/Pi 读取。
llm:
  model: deepseek-chat
  baseUrl: https://api.deepseek.com
  apiKeyEnv: NEWS_FEED_LLM_API_KEY
  mode: json
  timeoutMs: 120000
  maxRetries: 1
  validationRetries: 1
  summaryMaxTokens: 1024
  translationMaxTokens: 8192
